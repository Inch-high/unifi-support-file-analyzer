"""UniFi Support File Analyzer.

The version lives here and nowhere else. The git tag, the container image tags
and what the running program reports all derive from this string, and CI
refuses a release tag that disagrees with it, so a bug report naming a version
names something real.
"""
__version__ = "0.1.0"
